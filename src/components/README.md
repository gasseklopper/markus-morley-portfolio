# Components

Storybook documents the reusable UI pieces in this project. Components with
stories are listed below:

- **Button** (`button/`) – basic button component used across the site.

Each story lives next to its component file and uses the `*.stories.tsx` naming
convention. Run `npm run storybook` to explore them in the Storybook UI.
